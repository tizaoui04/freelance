{% extends 'back.html.twig' %}

{% block body %}

<div class="row">
    <div class="col-xl-6 col-md-6">

    <h1>Categorie creation</h1><br>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
        {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('categorie_index') }}">Back to the list</a>
        </li>
    </ul>
        </div>
</div>
{% endblock %}
